# Tests

All tests are defined in this directory. Each test type gets its own directory. Each test will contain a `profile.py` file. This contains a test function which takes `schedulerName`, and `results_object` as arguments. It runs the test for the given scheduler name, either default or custom, and logs the results to the results_object.