# Tests

All tests are defined in this directory. Each test type gets its own directory. This will contain a python dict which can be converted into profile class, and a manifest which is used in the tests. Results of the test will also be logged in these directories. There may also be custom scripts for interpreting the data in these directories.